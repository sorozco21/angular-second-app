import { FileSizeValidatorDirective } from './file-size-validator.directive';

describe('FileSizeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FileSizeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
